
name: ci

on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 * * * *"

jobs:
  autogreen:
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v2

      - name: Random
        id: random
        run: echo "::set-output name=bool::$(($RANDOM % 2))"

      - name: Auto green
        if: steps.random.outputs.bool == '1'
        run: |
          git config --local user.email "Xistorg@Gmail.com"
          git config --local user.name "Xistorg"
          git remote set-url origin https://${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}
          git pull --rebase
          git commit --allow-empty -m "a commit a day keeps your girlfriend away"
          git push
